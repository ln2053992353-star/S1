# smart_search_project 依赖清单
# 基于代码分析生成的关键依赖

# Django框架
Django>=4.2

# 数据库连接
pymysql
pypdf
mysqlclient>=2.2.0
cryptography>=46.0.0  # pymysql需要，用于MySQL caching_sha2_password认证

# AI/机器学习相关
sentence-transformers>=2.2.0
numpy>=1.21.0
torch>=2.0.0  # sentence-transformers依赖

# 数据处理
pandas>=1.3.0

# 网络请求
requests>=2.26.0

# 环境变量管理
python-dotenv>=1.0.0

# 其他可能需要的包（根据实际使用情况添加）
transformers>=4.30.0  # sentence-transformers依赖
scikit-learn>=1.0.0  # 可能用于相似度计算
faiss-cpu>=1.7.0  # 向量搜索

# LangGraph workflow (师姐的推荐系统)
langgraph>=0.2.0
langchain-groq>=0.2.0
chromadb>=0.5.0

# 安装命令: pip install -r requirements.txt
# 或使用国内镜像: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple